import React from 'react';
import TaskCard from './../../../components/DayView/TaskCard.js';
// import renderer from 'react-test-renderer';

test('Successfully converts hex values to RGBA'), () => {
  // let result = TaskCard.hexToRGBA('#000000');
  // expect(result).toBe('rgba(0,0,0,0.4');
}